language: r
r:
  - release
  - devel
sudo: false
cache: packages

r_packages:
  - knitr
  - devtools
  - methods

after_success:
  - Rscript -e 'covr::codecov()'

notifications:
  email:
    - amahzarn@uncc.edu
